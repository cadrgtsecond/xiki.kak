#!/bin/execlineb
multisubstitute {
    importas -iS HOME
    elgetpositionals -P 2
}
importas -SD ${HOME}/.config XDG_CONFIG_HOME
importas -SD ${XDG_CONFIG_HOME}/xiki XIKI_HOME

forbacktickx -E0x 0 script_path {
    foreground { find -L  ${XIKI_HOME}/scripts -type f -executable -print0 }
    printf %s/.*\\0 $XIKI_HOME
}
backtick -E script { basename $script_path }
case -N $1 {
    $script {
        backtick -E vars { pipeline { dollarat -n } awk "{ printf(\"export\\nARG%s\\n%s\\n\",NR,$0) }" }
        define -sd"
" var_split $vars
        $var_split
        $script_path $@
    }
}
false
